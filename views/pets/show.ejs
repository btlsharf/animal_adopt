<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pet Adoption Platform</title>
    <link rel="stylesheet" href="/styles/style.css">
</head>
<body>
    <%- include('navbar') %>
    <%- include('flash') %>
    <main class="container">
    <div class="pet-detail">
        <div class="pet-detail-header">
            <a href="/pets" class="back-link">← Back to all pets</a>
            
            <% if (currentUser && currentUser._id.toString() === pet.owner._id.toString()) { %>
                <div class="pet-actions">
                    <a href="/pets/<%= pet._id %>/edit" class="btn btn-secondary">Edit</a>
                    <form action="/pets/<%= pet._id %>?_method=DELETE" method="POST" style="display: inline;" onsubmit="return confirm('Are you sure you want to delete this pet listing?');">
                        <button type="submit" class="btn btn-danger">Delete</button>
                    </form>
                </div>
            <% } %>
        </div>
        
        <div class="pet-detail-content">
            <div class="pet-detail-image">
                <img src="<%= pet.imageUrl %>" alt="<%= pet.name %>">
            </div>
            
            <div class="pet-detail-info">
                <div class="pet-header">
                    <h1><%= pet.name %></h1>
                    <span class="status-badge <%= pet.status %>"><%= pet.status %></span>
                </div>
                
                <div class="pet-meta">
                    <div class="meta-item">
                        <strong>Species:</strong>
                        <span><%= pet.species.charAt(0).toUpperCase() + pet.species.slice(1) %></span>
                    </div>
                    
                    <% if (pet.breed) { %>
                    <div class="meta-item">
                        <strong>Breed:</strong>
                        <span><%= pet.breed %></span>
                    </div>
                    <% } %>
                    
                    <% if (pet.age) { %>
                    <div class="meta-item">
                        <strong>Age:</strong>
                        <span><%= pet.age %> <%= pet.age === 1 ? 'year' : 'years' %> old</span>
                    </div>
                    <% } %>
                    
                    <% if (pet.gender) { %>
                    <div class="meta-item">
                        <strong>Gender:</strong>
                        <span><%= pet.gender.charAt(0).toUpperCase() + pet.gender.slice(1) %></span>
                    </div>
                    <% } %>
                    
                </div>
                
                <div class="pet-description">
                    <h3>About <%= pet.name %></h3>
                    <p><%= pet.description %></p>
                </div>
                
                <div class="pet-owner">
                    <h3>Contact Information</h3>
                    <p><strong>Listed by:</strong> <%= pet.owner.username %></p>
                    <p><strong>Email:</strong> <%= pet.owner.email %></p>
                </div>
                
                <% if (currentUser && currentUser._id.toString() !== pet.owner._id.toString()) { %>
                    <div class="pet-favorite">
                        <% if (isFavorite) { %>
                            <form action="/favorites/<%= pet._id %>?_method=DELETE" method="POST">
                                <button type="submit" class="btn btn-secondary">❤️ Remove from Favorites</button>
                            </form>
                        <% } else { %>
                            <form action="/favorites/<%= pet._id %>" method="POST">
                                <button type="submit" class="btn btn-primary">🤍 Add to Favorites</button>
                            </form>
                        <% } %>
                    </div>
                <% } %>
            </div>
        </div>
    </div>
    </main>
        <footer class="footer">
            <div class="container">
                <p>&copy; 2025 Pet Adoption Platform. All rights reserved.</p>
            </div>
        </footer>
    </body>
    </html>